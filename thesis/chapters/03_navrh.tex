\chapter{Návrh řešení}
\label{chap:navrh}

V této kapitole je představen detailní technický návrh prototypu CLI klienta a navazující síťové služby. Návrh přímo vychází ze zvolené architektury tenkého klienta, která byla zdůvodněna v předchozí kapitole (viz sekce \ref{sec:analyza_architektury}).

\section{Komponenty systému}
Celý systém je v souladu se zvoleným návrhem rozdělen na dvě hlavní, technologicky odlišné komponenty, které spolu komunikují přes definované rozhraní.

\begin{itemize}
    \item \textbf{Služba (Service - .NET):} Funguje jako daemon spravovaný systémem \texttt{systemd} s právy uživatele \texttt{root}. Tato komponenta představuje jádro (backend) celého řešení. Její odpovědností je kompletní obsluha aplikační logiky – od komunikace s centrálním API GoodAccess, přes vyhodnocování stavů a bezpečné ukládání kryptografických klíčů, až po samotnou správu síťových rozhraní a konfiguraci tunelů WireGuard. Volba platformy .NET pro tuto komponentu přímo vychází z potřeby multiplatformní podpory a snadné integrace do systému \texttt{systemd} (viz sekce \ref{sec:technologie}), a především umožňuje znovupoužití již existující a otestované logiky z grafického klienta (viz sekce \ref{sec:analyza_architektury}).
    \item \textbf{Klient (Client - Go):} Běží v uživatelském prostoru (bez \texttt{root} oprávnění) a zajišťuje čistě interakci s uživatelem. Zodpovídá za parsování argumentů příkazové řádky pomocí frameworku \texttt{Cobra}, obsluhu interaktivních TUI prvků prostřednictvím \texttt{Bubble Tea} a finální formátování výstupu. Jazyk Go byl pro vývoj CLI zvolen díky svým vlastnostem pro systémové programování, rychlému startu a statické kompilaci do jediné binárky, což radikálně zjednodušuje distribuci (viz sekce \ref{sec:technologie} a \ref{sec:cli_teorie}).
\end{itemize}

Architektura Go klienta navíc úzce reflektuje principy takzvané \textit{Clean Architecture}~\cite{martin_clean}. Celá kódová základna klienta je rozdělena do logických vrstev, kde uživatelské rozhraní (UI a parsování příkazů) je zcela odděleno od aplikační logiky a transportní vrstvy (IPC komunikace přes Unix Domain Sockets). Toto vrstvení zaručuje vysokou testovatelnost komponent a možnost nezávisle upravovat vizuální prezentaci bez vlivu na způsob, jakým klient komunikuje s .NET službou.


\begin{figure}[H]
    \centering
    \begin{tikzpicture}[font=\sffamily\small, scale=1.1, transform shape, >=latex]
        % Styles
        \tikzset{
            component/.style={draw, thick, fill=white, align=center, minimum width=4cm, minimum height=1.5cm},
            database/.style={cylinder, draw, thick, aspect=0.25, shape border rotate=90, fill=white, align=center, minimum width=3cm, minimum height=1.8cm},
            cloudnode/.style={cloud, draw, thick, fill=white, cloud puffs=13, cloud puff arc=120, aspect=2.5, minimum width=4.5cm, minimum height=2.5cm, align=center},
            interface/.style={draw, circle, thick, fill=white, minimum size=0.3cm, inner sep=0}
        }

        % User Space Package
        \draw[thick, fill=gray!5] (-4.5, 4.5) -- (-1.5, 4.5) -- (-1.3, 4) -- (4.5, 4) -- (4.5, 2) -- (-4.5, 2) -- cycle;
        \draw[thick] (-4.5, 4) -- (-1.3, 4);
        \node[anchor=north west, font=\bfseries] at (-4.4, 4.5) {User Space};
        
        \node[component] (cli) at (0, 3) {$\ll$component$\gg$\\Go CLI Client};

        % System Space Package
        \draw[thick, fill=gray!5] (-6.5, -0.2) -- (-1.5, -0.2) -- (-1.3, -0.7) -- (6.5, -0.7) -- (6.5, -6.5) -- (-6.5, -6.5) -- cycle;
        \draw[thick] (-6.5, -0.7) -- (-1.3, -0.7);
        \node[anchor=north west, font=\bfseries] at (-6.4, -0.15) {System Space (root)};
        
        \node[component] (svc) at (0, -2.2) {$\ll$component$\gg$\\.NET GoodAccess Daemon\\(systemd service)};
        
        \node[component, minimum width=3.5cm] (wg) at (-3.5, -5.0) {$\ll$component$\gg$\\WireGuard\\Interface};
        \node[database] (sec) at (3.5, -5.0) {$\ll$database$\gg$\\Secure Storage\\Data Protection};

        % Cloud Node
        \node[cloudnode, fill=gray!5, minimum height=3.5cm] (cloud) at (0, -9.5) {};
        \node[anchor=north, font=\bfseries] at (0, -6.9) {GoodAccess Cloud};
        \node[component, minimum width=3cm, minimum height=1.2cm] (api) at (0, -9.5) {$\ll$component$\gg$\\GoodAccess API};

        % Unix Domain Socket (Interface)
        \node[interface] (uds) at (0, 0.75) {};
        \node[right=0.2cm of uds, align=left] {Unix Domain Socket\\\texttt{/tmp/CoreFxPipe\_ga-cli.sock}};
        
        % Connections
        \draw[thick] (svc) -- (uds); % Provided interface
        \draw[thick, dashed, ->] (cli) -- (uds) node[midway, left] {Raw JSON payload}; % Required interface
        
        \draw[thick, dashed, ->] (svc) -- (wg) node[midway, left=0.1cm] {Netlink API};
        \draw[thick, dashed, <->] (svc) -- (sec) node[midway, right] {R\&W};
        \draw[thick, dashed, ->] (svc) -- (api) node[midway, right] {HTTPS };

    \end{tikzpicture}
    \caption{Architektura systému znázorňující hranici mezi .NET Službou a Go Klientem skrze IPC (Unix Domain Sockets).}
    \label{fig:architecture_ipc}
\end{figure}

%\begin{figure}[H]
%    \centering
%    \includegraphics[width=0.8\textwidth]{images/clean-architecture.png}
%    \caption{Struktura vrstev Go klienta v souladu s principy Clean Architecture.}
%    \label{fig:clean_architecture_go}
%\end{figure}

\section{Návrh komunikace IPC}
\label{sec:navrh_ipc}
Pro výměnu dat mezi Go klientem a .NET službou bylo nutné zvolit vhodný mechanismus meziprocesní komunikace (IPC). Jak bylo popsáno v teoretické části (viz sekce \ref{sec:uds_theory}), pro lokální komunikaci v systému Linux jsou nejvhodnějším nástrojem Unix Domain Sockets (UDS). Ty nabízejí vyšší bezpečnost díky souborovým oprávněním a lepší výkon oproti síťovým socketům (TCP/IP), protože nedochází k režii spojené se síťovým stackem.

\subsection{Volba formátu dat}
Pro strukturování přenášených zpráv v rámci lokální komunikace mezi systémovými nástroji byl zvolen formát \textbf{JSON} \cite{rfc_json}. Hlavním důvodem pro jeho volbu je textová čitelnost, která výrazně usnadňuje manuální diagnostiku a ladění komunikace (např. pomocí nástrojů jako \texttt{socat}) bez nutnosti binární dešifrace zpráv.

Dalším faktorem je nativní a velmi robustní podpora serializace do JSON formátu ve standardních knihovnách obou použitých jazyků (Go i C\#), díky čemuž odpadá nutnost využívat dodatečné mezivrstvy či složité procesy pro generování kódu. Tato volba představuje optimální kompromis mezi technickou efektivitou a vývojovou udržitelností, což odpovídá agilnímu principu jednoduchosti.

\subsection{Protokol a struktura zpráv}
Komunikace probíhá podle modelu Request-Response. Klient otevře spojení k socketu služby (typicky \texttt{/tmp/\allowbreak{}CoreFxPipe\_ga-cli.sock}), odešle požadavek v JSON formátu a čeká na odpověď, po které spojení uzavře.

Průběh typické komunikace pro přihlášení uživatele znázorňuje následující sekvenční diagram (Obrázek \ref{fig:ipcsequence}).

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[font=\sffamily\small, >=latex]
        % Lifelines nodes
        \node (user) at (0, 0) {\textbf{User}};
        \node[draw, thick, fill=white, minimum width=2.5cm, minimum height=0.8cm] (cli) at (3.5, 0) {\textbf{Go CLI Client}};
        \node[draw, thick, fill=white, minimum width=2.5cm, minimum height=0.8cm] (uds) at (7.5, 0) {\textbf{UDS Socket}};
        \node[draw, thick, fill=white, minimum width=2.5cm, minimum height=0.8cm] (svc) at (11.5, 0) {\textbf{.NET Daemon}};

        % Lifelines lines
        \draw[thick, dashed] (0, -0.4) -- (0, -9.5);
        \draw[thick, dashed] (3.5, -0.4) -- (3.5, -9.5);
        \draw[thick, dashed] (7.5, -0.4) -- (7.5, -9.5);
        \draw[thick, dashed] (11.5, -0.4) -- (11.5, -9.5);

        % Execution bars
        \draw[thick, fill=blue!10] (3.35, -1.0) rectangle (3.65, -9.2);
        \draw[thick, fill=gray!10] (7.35, -3.2) rectangle (7.65, -8.2);
        \draw[thick, fill=red!10] (11.35, -4.2) rectangle (11.65, -7.2);

        % Messages
        \draw[->, thick] (0, -1.2) -- (3.35, -1.2) node[midway, above] {\texttt{ga login}};

        % CLI self message
        \draw[->, thick] (3.65, -1.4) -- (4.5, -1.4) -- (4.5, -2.2) -- (3.65, -2.2) node[midway, right=0.4cm, align=left] {Prompt for\\credentials};

        % CLI to UDS
        \draw[->, thick] (3.65, -3.4) -- (7.35, -3.4) node[midway, above] {Write JSON Request};
        
        % UDS to SVC
        \draw[->, thick] (7.65, -4.4) -- (11.35, -4.4) node[midway, above] {Transmit byte stream};
        
        % SVC self messages
        \draw[->, thick] (11.65, -4.8) -- (12.6, -4.8) -- (12.6, -5.8) -- (11.65, -5.8) node[midway, right=0.4cm, align=left] {Validate request,\\call GoodAccess API,\\save tokens to storage};
        
        % SVC to UDS
        \draw[->, thick, dashed] (11.35, -7.0) -- (7.65, -7.0) node[midway, above] {Write JSON Response};
        
        % UDS to CLI
        \draw[->, thick, dashed] (7.35, -8.0) -- (3.65, -8.0) node[midway, above] {Transmit byte stream};
        
        % CLI to User
        \draw[->, thick, dashed] (3.35, -9.0) -- (0, -9.0) node[midway, above] {Display successful login};

    \end{tikzpicture}
    \caption{Sekvenční diagram IPC komunikace při přihlášení.}
    \label{fig:ipcsequence}
\end{figure}

\subsection{Příklady zpráv}
Struktura požadavku obsahuje název příkazu (\texttt{command}), samotná data (\texttt{payload}) a kontext požadavku (\texttt{context}), který nese informace o volajícím uživateli.

Příklad požadavku na přihlášení (\texttt{login}):

\begin{minipage}{\linewidth}
\begin{lstlisting}[language={}]
{
  "command": "login",
  "payload": {
    "TeamName": "mojefirma",
    "UserName": "jan.novak@mojefirma.cz",
    "Password": "moje_tajne_heslo"
  },
  "context": {
    "LinuxId": "1000",
    "IsRoot": false
  }
}
\end{lstlisting}
\end{minipage}

Služba na tento požadavek odpovídá strukturovanou zprávou, která indikuje úspěch či selhání a vrací příslušná data nebo chybové hlášení.

Příklad úspěšné odpovědi:

\begin{minipage}{\linewidth}
\begin{lstlisting}[language={}]
{
  "success": true,
  "data": {
    "IsLoggedIn": true
  }
}
\end{lstlisting}
\end{minipage}

Příklad odpovědi při chybě (neplatné údaje):

\begin{minipage}{\linewidth}
\begin{lstlisting}[language={}]
{
  "success": false,
  "data": null,
  "error": "Invalid credentials"
}
\end{lstlisting}
\end{minipage}

\section{Návrh uživatelského rozhraní (CLI)}
Uživatelské rozhraní aplikace je navrženo s důrazem na dvě odlišné skupiny uživatelů: lidi, kteří vyžadují interaktivní a přehledné prostředí pro každodenní práci, a stroje (skripty, CI/CD systémy), které potřebují predikovatelný a snadno parsovatelný výstup. Tento duální přístup přímo reflektuje funkční požadavky stanovené v analytické části (viz sekce \ref{sec:funkcni_pozadavky}).

\subsection{Strom příkazů}
Struktura příkazů je postavena na frameworku \texttt{Cobra} (viz sekce \ref{sec:technologie}), který umožňuje definovat hierarchický model ovládání. Hlavní binární soubor \texttt{ga-cli} slouží jako vstupní bod, pod kterým jsou organizovány jednotlivé funkční celky formou sub-příkazů. Tato struktura zajišťuje intuitivní ovládání a snadnou rozšiřitelnost o budoucí funkce.

Aktuální hierarchii příkazů znázorňuje Obrázek \ref{fig:command_tree}.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        font=\sffamily\small,
        cmd/.style={draw, thick, fill=blue!5, rounded corners, minimum width=2.5cm, minimum height=0.8cm},
        flag/.style={draw, dashed, thick, fill=gray!10, rounded corners, minimum width=2.5cm, minimum height=0.6cm, font=\ttfamily\footnotesize},
        >=latex
    ]

    % Commands   
    \node[cmd] (login) at (0, 3.3) {login};
    \node[cmd] (logout) at (0, 2.2) {logout};
    \node[cmd] (setup) at (0, 1.1) {setup};
    \node[cmd] (connect) at (0, 0) {connect};
    \node[cmd] (disconnect) at (0, -1.1) {disconnect};
    \node[cmd] (status) at (0, -2.2) {status};
    \node[cmd] (version) at (0, -3.3) {version};

    % Root node
    \node[cmd, fill=blue!15, font=\sffamily\bfseries] (root) at (-4, 0) {ga-cli};

    % Flags
    \node[flag] (setup-g) at (4, 1.1) {-{}-gateway};
    \node[flag] (connect-g) at (4, 0.45) {-{}-gateway};
    \node[flag] (connect-p) at (4, -0.45) {-{}-protocol};
    \node[flag] (status-j) at (4, -2.2) {-{}-json};
    \node[flag] (version-j) at (4, -3.3) {-{}-json};

    % Connect root to commands
    \draw[thick] (root.east) -- (-2, 0) coordinate (bus);
    \draw[->, thick] (bus) |- (login.west);
    \draw[->, thick] (bus) |- (logout.west);
    \draw[->, thick] (bus) |- (setup.west);
    \draw[->, thick] (bus) -- (connect.west);
    \draw[->, thick] (bus) |- (disconnect.west);
    \draw[->, thick] (bus) |- (status.west);
    \draw[->, thick] (bus) |- (version.west);

    % Connect commands to flags
    \draw[->, thick, dashed] (setup.east) -- (setup-g.west);
    
    \draw[thick, dashed] (connect.east) -- (2, 0) coordinate (cbus);
    \draw[->, thick, dashed] (cbus) |- (connect-g.west);
    \draw[->, thick, dashed] (cbus) |- (connect-p.west);
    
    \draw[->, thick, dashed] (status.east) -- (status-j.west);
    \draw[->, thick, dashed] (version.east) -- (version-j.west);

    \end{tikzpicture}
    \caption{Hierarchická struktura příkazů (Command Tree) aplikace ga-cli.}
    \label{fig:command_tree}
\end{figure}

Mezi klíčové příkazy patří:
\begin{itemize}
    \item \texttt{login}: Pro autentizaci uživatele vůči backendu GoodAccess.
    \item \texttt{setup}: Komplexní průvodce prvotním nastavením (viz sekce \ref{sec:setup_flow}).
    \item \texttt{connect}: Navázání VPN připojení. Bez parametrů se připojuje k preferované bráně. Pomocí přepínače \texttt{-g} nebo \texttt{-{}-gateway} lze vyvolat interaktivní výběr pro jednorázové připojení k jiné bráně.
    \item \texttt{disconnect}: Bezpečné ukončení aktivního tunelu.
    \item \texttt{status}: Zobrazení detailních informací o aktuální relaci.
\end{itemize}

\subsection{Vizuální styl a UX principy}
\label{sec:vizualni_styl}
Pro zajištění konzistentního a profesionálního vzhledu v terminálu využívá aplikace knihovnu \texttt{Lip Gloss} pro definici stylů a barevné palety. Zvolené barvy nejsou náhodné, ale plní specifickou sémantickou roli:

\begin{itemize}
    \item \textcolor[HTML]{7D56F4}{\textbf{Primary (Fialová \texttt{\#7D56F4}):}} Použita pro hlavní značku aplikace, nadpisy a zvýraznění klíčových informací.
    \item \textcolor[HTML]{04B575}{\textbf{Secondary (Zelená \texttt{\#04B575}):}} Indikuje úspěšné operace a stav „Připojeno“.
    \item \textcolor[HTML]{FF0000}{\textbf{Error (Červená \texttt{\#FF0000}):}} Kritické chyby a neúspěšné validace.
    \item \textcolor[HTML]{FFA500}{\textbf{Warning (Oranžová \texttt{\#FFA500}):}} Varování, která nevyžadují okamžité ukončení, ale vyžadují pozornost uživatele.
    \item \textcolor[HTML]{626262}{\textbf{Subtle (Šedá \texttt{\#626262}):}} Doplňkové informace a nápovědy, které nemají odvádět pozornost od hlavního toku dat.
\end{itemize}

Z pohledu uživatelské zkušenosti (UX) je kladen důraz na okamžitou odezvu. Každá operace trvající déle než 100\,ms je doprovázena animovaným prvkem (spinner), aby uživatel věděl, že aplikace „nezamrzla“. Dále aplikace využívá kontextové nápovědy (hints), které uživateli po dokončení příkazu navrhují další logický krok (např. po úspěšném přihlášení navrhne spuštění \texttt{ga-cli setup} nebo \texttt{ga-cli connect}).

\subsection{Průvodce nastavením (Setup)}
\label{sec:setup_flow}
Specifickým prvkem návrhu je příkaz \texttt{ga-cli setup}. Jedná se o tzv. „vše v jednom“ flow, které je navrženo pro maximální pohodlí při prvním spuštění aplikace. Průvodce lineárně provede uživatele následujícími kroky:
\begin{enumerate}
    \item Autentizace (pokud uživatel není přihlášen).
    \item Výběr preferovaného VPN protokolu (WireGuard / OpenVPN).
    \item Interaktivní výběr výchozí brány (gateway).
    \item Dotaz na aktivaci perzistentního připojení (Auto-connect).
    \item Okamžité navázání spojení s nově uloženou konfigurací.
\end{enumerate}
Tento přístup radikálně snižuje bariéru vstupu pro nové uživatele, kteří by jinak museli studovat manuálové stránky jednotlivých příkazů.

\section{Správa dat a bezpečnostní model}
\label{sec:data_a_bezpecnost}

Jelikož je navržený systém rozdělen na privilegovanou službu a neprivilegovaného klienta, je nutné zajistit bezpečné úložiště pro citlivá data (zejména autentizační tokeny) a zabezpečit meziprocesovou komunikaci. Tyto dvě oblasti spolu úzce souvisejí, a proto jsou v návrhu řešeny komplexně.

\subsection{Perzistence a struktura dat}

Veškerý perzistentní stav aplikace je udržován systémovou službou (daemonem), zatímco CLI klient zůstává striktně bezestavový. Datový model je definován kořenovou strukturou, která sdružuje globální konfiguraci (\texttt{GlobalConfig}), aktivní uživatelské relace obsahující relační tokeny (\texttt{ActiveSessions}) a lokální mezipaměť známých bran (\texttt{KnownGateways}). Tato datová struktura je při ukládání serializována do textového formátu JSON. Aby však nedocházelo k úniku citlivých dat (například při neoprávněném čtení souborového systému jiným procesem či uživatelem), je tento textový řetězec před samotným zápisem na disk kryptograficky chráněn.

\subsection{Implementace zabezpečeného úložiště (Secure Storage)}

Pro ochranu perzistentních dat bylo využito rozhraní \texttt{Data Protection API}, které je nativní součástí platformy ASP.NET Core (jak bylo teoreticky popsáno v sekci \ref{sec:technologie}). Toto rozhraní zajišťuje transparentní šifrování (\texttt{Protect}) a dešifrování (\texttt{Unprotect}) ukládaných databázových souborů.

Klíčovým bezpečnostním prvkem celého mechanismu je správa samotných šifrovacích klíčů. Ty jsou generovány a izolovány ve vyhrazeném adresáři (konkrétně \texttt{/opt/\allowbreak{}GoodAccess/\allowbreak{}configs/\allowbreak{}DataProtection-Keys}). Při startu služby je programově zajištěno nastavení striktních přístupových práv (v prostředí Linuxu pomocí příkazu \texttt{chmod 700}). Tím je na úrovni operačního systému garantováno, že k šifrovacím klíčům – a potažmo k uloženým datům – má přístup výhradně uživatel \texttt{root}, pod nímž je služba běžně spuštěna. Celková architektura bezpečnostní komponenty je znázorněna na obrázku \ref{fig:security_component}.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[font=\sffamily\small, scale=1.0, transform shape, >=latex]
        % Styles
        \tikzset{
            component/.style={draw, thick, fill=white, align=center, minimum width=4cm, minimum height=1.2cm},
            database/.style={cylinder, draw, thick, aspect=0.25, shape border rotate=90, fill=white, align=center, minimum width=3.5cm, minimum height=1.5cm},
            file/.style={draw, thick, fill=white, align=center, minimum width=3.5cm, minimum height=1.5cm},
            interface/.style={draw, circle, thick, fill=white, minimum size=0.3cm, inner sep=0}
        }

        % User Space Package
        \draw[thick, fill=gray!5] (-5.5, 6) -- (-1.5, 6) -- (-1.3, 5.5) -- (5.5, 5.5) -- (5.5, 3.5) -- (-5.5, 3.5) -- cycle;
        \draw[thick] (-5.5, 5.5) -- (-1.3, 5.5);
        \node[anchor=north west, font=\bfseries] at (-5.4, 6) {User Space (UID 1000)};
        
        \node[component] (cli) at (0, 4.5) {$\ll$component$\gg$\\Go CLI Client};

        % System Space Package
        \draw[thick, fill=gray!5] (-7.0, 2) -- (-1.5, 2) -- (-1.3, 1.5) -- (7.0, 1.5) -- (7.0, -7.5) -- (-7.0, -7.5) -- cycle;
        \draw[thick] (-7.0, 1.5) -- (-1.3, 1.5);
        \node[anchor=north west, font=\bfseries] at (-6.9, 2) {System Space (root)};
        
        % UDS Interface
        \node[interface] (uds) at (0, 2) {};
        \node[right=0.2cm of uds, align=left] {Unix Domain Socket\\\texttt{/tmp/CoreFxPipe\_ga-cli.sock}};

        % Svc Component Box
        \draw[thick, fill=blue!5, rounded corners=2pt] (-3.2, 0.7) rectangle (3.2, -3.8);
        \node[anchor=south, font=\bfseries] at (0, 0.7) {$\ll$component$\gg$ .NET GoodAccess Service};

        \node[component, minimum width=5cm] (auth) at (0, -0.2) {$\ll$component$\gg$\\IPC Middleware\\(LinuxId Validation)};
        \node[component, minimum width=5cm] (dp) at (0, -2.6) {$\ll$component$\gg$\\Data Protection API\\(Encryption Engine)};

        % Storage and Keys
        \node[database] (storage) at (0, -6.2) {$\ll$database$\gg$\\Persistent Storage\\(Encrypted JSON)};
        \node[file] (keys) at (-5.2, -2.6) {$\ll$file$\gg$\\Encryption Keys\\\texttt{chmod 700}};

        % Connections
        \draw[thick, dashed, ->] (cli) -- (uds) node[midway, right] {Message with Context LinuxId};
        \draw[thick] (auth) -- (uds); % Provided interface
        
        \draw[thick, ->] (auth) -- (dp) node[midway, right, align=left] {R\&W requests};
        \draw[thick, dashed, ->] (dp) -- (keys) node[midway, above] {Load Key};
        \draw[thick, dashed, ->] (dp) -- (storage) node[midway, right] {Plain-text to Cipher-text};

    \end{tikzpicture}
    \caption{Architektura zabezpečeného úložiště a správy klíčů.}
    \label{fig:security_component}
\end{figure}

\subsection{Bezpečnost meziprocesové komunikace (IPC)}

Komunikační kanál mezi klientem a službou je realizován pomocí pojmenovaných rour (Named Pipes), které jsou v prostředí operačního systému Linux implementovány jako Unix Domain Sockets \cite{dotnet_named_pipes} (v tomto návrhu konkrétně umístěné v systémovém adresáři \texttt{/tmp/\allowbreak{}CoreFxPipe\_ga-cli.sock}). Místo zavádění dedikovaných uživatelských skupin pro omezení přístupu k soketu na úrovni souborového systému je bezpečnost řešena dynamicky přímo na aplikační úrovni služby.

Každý požadavek odeslaný z CLI klienta obsahuje kromě samotných uživatelských dat (payload) také aplikační kontext s identifikátorem volajícího uživatele (\texttt{LinuxId}). Služba tak může při zpracování požadavku bezpečně ověřovat, zda má uživatel dostatečná oprávnění k požadované akci (např. manipulaci s VPN připojením, odhlášení relace). V případě zjištění narušení bezpečnosti nebo nedostatečných oprávnění je požadavek okamžitě zamítnut, čímž je zajištěna integrita systému i při sdílení jednoho soketu více lokálními uživateli.

\section{Návrh distribuce a aktualizací}
\label{sec:distribuce_aktualizace}

Díky cílení na platformu operačního systému Linux bylo nezbytné navrhnout distribuční a aktualizační mechanismus tak, aby ctil zavedené standardy a integraci se systémovými nástroji (viz sekce \ref{sec:distribuce}). Návrh se proto cíleně vyhýbá mechanismu automatických vnitřních aktualizací (tzv. self-update), který je běžný například v prostředí Windows, a místo toho se plně spoléhá na nativní správce balíčků.

\subsection{Distribuční balíčky a instalace}

Aplikace bude distribuována ve dvou standardních formátech, které pokrývají drtivou většinu cílového trhu:
\begin{itemize}
    \item \textbf{.deb balíčky} pro distribuce vycházející z rodiny Debian (např. Ubuntu, Linux Mint).
    \item \textbf{.rpm balíčky} pro rodinu distribucí založených na Red Hat (např. Fedora, CentOS, RHEL).
\end{itemize}

Instalační balíček funkčně zapouzdřuje obě klíčové komponenty systému. Během instalačního procesu (např. při volání \texttt{apt-get install}) dojde k umístění zkompilované binární aplikace klienta (napsané v jazyce Go) do systémového adresáře \texttt{/usr/\allowbreak{}bin/\allowbreak{}ga-cli}, čímž se stane globálně dostupnou pro všechny uživatele systému. Zároveň jsou do adresáře \texttt{/opt/\allowbreak{}GoodAccess/} rozbaleny veškeré závislosti a samotná .NET služba. Součástí instalace je také registrace příslušného konfiguračního souboru (unit file) pro inicializační systém \texttt{systemd}. Instalační skript balíčku (post-install) následně zajistí automatické spuštění služby na pozadí, čímž je systém okamžitě připraven k použití.

\subsection{Detekce a provedení aktualizace}

Ačkoliv je samotný proces povýšení verze (upgrade) ponechán na operačním systému, aplikace aktivně detekuje dostupnost nových vydání, aby zajistila informovanost uživatele. Tato proaktivní detekce je implementována v rámci příkazu \texttt{ga-cli version}. Služba při zavolání asynchronně dotazuje centrální API \mbox{GoodAccess}. Pokud odpověď ze serveru indikuje dostupnost novější verze (v datovém modelu reprezentováno příznakem \texttt{UpdateAvailable}), CLI klient tuto skutečnost vizuálně zdůrazní ve standardním výstupu a doporučí uživateli provést systémovou aktualizaci.

\textbf{Bezpečnostní a architektonický přínos:} Tento model striktně odděluje informační povinnost klienta od výkonné moci nad souborovým systémem. Neprivilegovaný uživatel je upozorněn na novou verzi, avšak samotný přepis spustitelných souborů a nezbytný restart daemonu může provést pouze systémový administrátor (\texttt{root}) standardním voláním systémového správce balíčků (např. \texttt{sudo apt-get update \&\& sudo apt-get upgrade goodaccess-cli}). Tím je na architektonické úrovni zamezeno případnému narušení bezpečnosti (privilege escalation) skrze potenciální zranitelnosti v aktualizačním procesu samotné aplikace.
