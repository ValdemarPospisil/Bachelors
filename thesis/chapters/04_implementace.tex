\chapter{Implementace}
\label{chap:implementace}

V této kapitole je detailně rozebrán samotný proces implementace navrženého řešení. Pozornost je věnována struktuře celého projektu a následně specifickým technickým výzvám spojeným s vývojem klientské aplikace v jazyce Go a úpravám systémové služby na platformě .NET.

\section{Struktura projektu a vývojové prostředí}
Jelikož se výsledný produkt skládá z několika oddělených, avšak úzce spolupracujících částí, bylo zásadní správně navrhnout strukturu repozitáře. Repozitář je koncipován jako monorepo, které zastřešuje zdrojové kódy pro všechny klientské i systémové komponenty. Tento přístup výrazně zjednodušuje správu závislostí a zajišťuje konzistentní verzování napříč celým projektem.

Hlavní rozdělení kořenového adresáře odráží oddělení jednotlivých technologických a logických domén:

\begin{itemize}
    \item \texttt{client/} -- Tato složka představuje zdrojové kódy původního grafického klienta postaveného na technologiích Electron a React. Byť není primárním předmětem této práce, jeho přítomnost v repozitáři usnadňuje sdílení prostředků.
    
    \item \texttt{cli/} -- Zde se nachází kompletní implementace nového terminálového klienta (frontendu) v jazyce Go.
    \begin{itemize}
        \item \texttt{cmd/} -- Obsahuje definice jednotlivých CLI příkazů využívajících framework Cobra (např. \texttt{login.go}, \texttt{connect.go}, \texttt{status.go}). Soubor \texttt{root.go} slouží jako vstupní bod pro parsování uživatelských vstupů.
        \item \texttt{internal/} -- Zapouzdřuje interní logiku aplikace, kterou nelze importovat z externích balíčků.
        \begin{itemize}
            \item \texttt{ipc/} -- Implementuje komunikační vrstvu, konkrétně klienta pro Unix Domain Sockets a definici zpráv (tzv. \textit{protocol}).
            \item \texttt{tui/} -- Zastřešuje interaktivní textové uživatelské rozhraní postavené na frameworku Bubble Tea. Každá obrazovka (např. \texttt{login/}, \texttt{connect/}) je zde oddělena do vlastní složky obsahující modely (\texttt{model.go}), zpracování událostí (\texttt{update.go}) a vykreslovací logiku (\texttt{view.go}).
        \end{itemize}
    \end{itemize}

    \item \texttt{services/} -- Obsahuje .NET projekty představující backend vrstvu, neboli systémové služby běžící na pozadí s vyššími právy.
    \begin{itemize}
        \item \texttt{CLIService/} -- Zcela nově vytvořený projekt v rámci této práce. Funguje jako dedikovaná \textit{wrapper} služba (tzv. obálka) pro CLI klienta. Hlavním úkolem \texttt{CLIService} je naslouchat na IPC socketu (\texttt{SenderReader.cs}), přijímat požadavky od frontend klienta a směrovat je pomocí komponenty \texttt{CliMessenger.cs} k příslušným výkonným vrstvám.
        \item \texttt{GoodAccess.Shared/} -- Obsahuje sdílenou doménovou logiku, kterou využívají různé části backendu. Nachází se zde klientské třídy pro komunikaci s centrálním REST API (\texttt{ApiClient.cs}) a abstraktní implementace VPN protokolů (\texttt{ProtoService.cs}, \texttt{VpnManager.cs}).
        \item \texttt{GoodAccessService/} -- Původní a hlavní jádro síťové infrastruktury (daemon). Využívá konfigurace z předchozích vrstev k fyzickému sestavení a správě tunelů na daném operačním systému.
    \end{itemize}
\end{itemize}

Tato architektura je tak striktně rozdělena na takzvaného tenkého klienta (Thin Client), jenž je zodpovědný výhradně za prezentaci dat a odesílání uživatelských povelů, a robustní systémovou službu, která si ponechává komplexní aplikační logiku. Navíc obě tyto logické vrstvy (Go aplikace a .NET služby) mají vlastní nezávislé procesy pro sestavení a distribuci.

Pro názornější představu o komplexnosti řešení a vzájemných vazbách jsou níže uvedeny dvě doplňující vizualizace. Obrázek~\ref{fig:mindmap_structure} nabízí pohled na strukturu repozitáře ve formě myšlenkové mapy, zatímco Obrázek~\ref{fig:wbs_structure} představuje stejnou hierarchii formou WBS (Work Breakdown Structure) diagramu pro detailnější dekompozici jednotlivých submodulů.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/implementation/diagrams/mindmap-file-structure.png}
    \caption{Struktura repozitáře (Mindmap)}
    \label{fig:mindmap_structure}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/implementation/diagrams/wbs-file-structure.png}
    \caption{Struktura repozitáře (WBS)}
    \label{fig:wbs_structure}
\end{figure}

\section{Implementace systémové služby (.NET)}
Aby bylo možné efektivně zpracovávat požadavky přicházející od CLI klienta, bylo nutné infrastrukturu na straně služby patřičně rozšířit. Vývoj probíhal iterativně počínaje zajištěním bezpečného úložiště a zprovozněním lokálního komunikačního kanálu.

\subsection{Bezpečné úložiště (Secure Storage)}
Aplikace potřebuje uchovávat citlivá data jako jsou autentizační tokeny a privátní klíče. Jak bylo definováno v kapitole~\ref{sec:data_a_bezpecnost}, původní implementace pro grafické rozhraní ukládala některá data do prostoru uživatele. Jelikož je CLI klient navržen s možností trvalého spojení běžícího čistě na úrovni systému, muselo být úložiště přesunuto do bezpečného systémového adresáře (\texttt{/opt/GoodAccess/configs/DataProtection-Keys}).

Byla vytvořena třída \texttt{CliStorage} reprezentující zabezpečené úložiště. Fyzické uložení dat využívá soubory zabezpečené restriktivními přístupovými právy, čímž je zaručeno, že k souborům má přístup pouze vlastník procesu (v tomto případě uživatel \texttt{root}, pod kterým služba běží). Samotná data uvnitř souborů jsou navíc šifrována na úrovni .NET frameworku pomocí \texttt{IDataProtectionProvider}. Třída umožňuje čtení a zápis serializovaných JSON objektů (\texttt{CliStorageRoot}) do tohoto chráněného souboru.

\noindent\begin{minipage}{\linewidth}
\lstinputlisting[language=C, stringstyle=\itshape, firstline=155, lastline=161]{../doc/code/CliStorage.cs}
\end{minipage}

\subsection{IPC Server (Unix Domain Sockets)}
Základním kamenem architektury je nově vytvořený modul \texttt{CLIService}. Tento modul slouží jako most mezi uživatelskými vstupy a jádrem služby. Jak bylo navrženo v kapitole~\ref{sec:navrh_ipc}, komunikace probíhá přes Unix Domain Sockets (UDS), což je na platformě Linux standardní a vysoce výkonný způsob výměny dat mezi lokálními procesy. Služba vytvoří pojmenovanou rouru (named pipe), na které asynchronně naslouchá příchozím spojením od klienta. Požadavky jsou zasílány a vraceny ve formátu JSON, což usnadňuje deserializaci dat na obou stranách.

\noindent\begin{minipage}{\linewidth}
\lstinputlisting[language=C, stringstyle=\itshape, firstline=36, lastline=42]{../doc/code/SenderReader.cs}
\end{minipage}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/implementation/diagrams/ipc-diagram.png}
    \caption{Architektura IPC komunikace}
    \label{fig:ipc_diagram_impl}
\end{figure}

\subsection{Zpracování příkazů (CliMessenger)}
Za samotné odbavování příchozích zpráv zodpovídá komponenta \texttt{CliMessenger}. Jak bylo definováno v podkapitole~\ref{sec:navrh_ipc} (včetně konkrétních JSON příkladů), každý požadavek ve formátu JSON obsahuje specifikaci příkazu (např. \texttt{login}, \texttt{connect}) a příslušné argumenty. \texttt{CliMessenger} tyto požadavky zpracovává a směruje je na specifické služby v rámci .NET backendu. Jak ukazuje následující ukázka kódu, třída pomocí konstrukce \texttt{switch} analyzuje přijatý příkaz a asynchronně volá odpovídající obslužné metody.

\noindent\begin{minipage}{\linewidth}
\lstinputlisting[language=C, stringstyle=\itshape, firstline=194, lastline=204]{../doc/code/CliMessenger.cs}
\end{minipage}

V raných fázích vývoje, kdy ještě nebyla zcela implementována veškerá produkční logika a klientská aplikace se teprve formovala, vracela metoda \texttt{CliMessenger} provizorní, pevně definované hodnoty (tzv. hardcoded data). Tento přístup umožnil paralelní vývoj -- uživatelské rozhraní se mohlo designovat a testovat proti konzistentnímu API bez čekání na dokončení komplexní aplikační logiky. Postupem času byly tyto provizorní odpovědi nahrazovány reálným napojením na síťové a autentizační subsystémy.

\subsection{Životní cyklus a integrace se systemd}
Pro zajištění běhu služby na pozadí a správnou instalaci na Linuxu byl využit modul \texttt{Microsoft.\allowbreak Extensions.\allowbreak Hosting.\allowbreak Systemd}. Díky tomuto modulu se celá aplikace může chovat jako nativní \texttt{systemd} služba. Definice \texttt{goodaccess-cli.service} zajišťuje automatické spuštění po startu systému a \textit{graceful shutdown} proces pro bezpečné odpojení VPN před vypnutím. Následující ukázka kódu z konfigurační třídy \texttt{Program.cs} demonstruje inicializaci hostitele, kde metoda \texttt{UseSystemd()} registruje životní cyklus služby a zároveň probíhá konfigurace \texttt{DataProtection} API z předchozí podkapitoly a registrace hlavní pracovní třídy \texttt{Worker}, která obstarává chod aplikace v pozadí.

\noindent\begin{minipage}{\linewidth}
\lstinputlisting[language=C, stringstyle=\itshape, firstline=33, lastline=46]{../doc/code/Program.cs}
\end{minipage}

\section{Implementace klientské aplikace (Go)}
Na straně klienta bylo zvoleno využití jazyka Go, a to především pro jeho vynikající vlastnosti při vývoji CLI aplikací a schopnost kompilace do jediné statické binární formy nezávislé na externích knihovnách na koncovém systému.

\subsection{Parsování příkazů (Cobra)}
Základní aplikační kostra byla postavena na populárním frameworku Cobra. Tento nástroj umožňuje velmi snadno deklarovat stromovou strukturu příkazů, definovat argumenty (tzv. flags) a automaticky generovat nápovědu pro uživatele. Stromová struktura návrhu byla prezentována již v kapitole~\ref{sec:technologie} (Obrázek~\ref{fig:command_tree}). Hlavní příkaz \texttt{ga-cli} byl inicializován jako kořenový uzel (Root Command), ke kterému byly postupně přidávány dílčí příkazy odpovídající požadovaným akcím (login, setup, connect a další). Architektura frameworku Cobra navíc vybízí k přehlednému uspořádání kódu, kdy má každý příkaz svůj vlastní soubor v adresáři \texttt{cmd/}.

Následující ukázka kódu ze souboru \texttt{connect.go} ilustruje typickou definici příkazu v Cobra frameworku. Vlastnost \texttt{Use} definuje, jak se příkaz volá z terminálu. \texttt{Short} a \texttt{Long} poskytují texty pro automaticky generovanou nápovědu (Obrázek~\ref{fig:ga_cli_help}). Samotná výkonná logika je pak svěřena funkci pod klíčem \texttt{RunE}, která v tomto případě inicializuje IPC klienta a odesílá požadavek na připojení.

\noindent\begin{minipage}{\linewidth}
\lstinputlisting[language=C, stringstyle=\itshape, firstline=18, lastline=24]{../doc/code/connect.go}
\end{minipage}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{images/implementation/screenshots/help-command.png}
    \caption{Automaticky generovaná nápověda hlavního příkazu}
    \label{fig:ga_cli_help}
\end{figure}

\subsection{Interaktivní terminálové rozhraní (Bubble Tea)}
Pro složitější interakce, jako je průvodce nastavením (Setup wizard), bylo využito textové uživatelské rozhraní (TUI) pomocí knihovny Bubble Tea. Jak bylo teoreticky rozebráno v kapitole~\ref{sec:elm_bubbletea}, tento framework implementuje \textit{The Elm Architecture}. Implementace využívá funkci \texttt{Update} pro reakci na klávesové vstupy a funkci \texttt{View} pro vykreslování vizuálních komponent na základě aktuálního stavu modelu. Celý onboarding proces tak působí na uživatele uceleným a přehledným dojmem.

Následující ukázka kódu z \texttt{view.go} demonstruje, jak je definován vzhled jednoho konkrétního kroku průvodce (Obrázek~\ref{fig:setup_connect_step}) – dotaz na navázání spojení. Lze si povšimnout využití balíčku \texttt{lipgloss} (zde pod aliasem \texttt{style}) pro aplikování barev a formátování textu, což výrazně zlepšuje čitelnost v terminálu.

\noindent\begin{minipage}{\linewidth}
\lstinputlisting[language=C, stringstyle=\itshape, firstline=154, lastline=169]{../doc/code/view.go}
\end{minipage}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.6\textwidth]{images/implementation/screenshots/setup-step-connect.png}
    \caption{Interaktivní krok průvodce nastavením pro navázání spojení}
    \label{fig:setup_connect_step}
\end{figure}

\subsection{Komunikace se službou (IPC Klient)}
Odesílání a přijímání JSON dat z Unix Domain Socketu je zajištěno IPC klientem na straně Go aplikace. Klient navazuje spojení přes systémové volání s definovaným \texttt{unix} socketem. Pro zajištění konzistence je každá odesílaná zpráva zabalena do tzv. \textit{Request Envelope}. Tato obálka (implementovaná jako Go struktura) obsahuje nejen samotný povel, ale také kontextové informace, jako je ID Linuxového uživatele, který příkaz vyvolal, a flag určující, zda byl příkaz spuštěn s právy administrátora (root). Tyto údaje jsou nezbytné pro správné ověření oprávnění na straně služby (viz podkapitola 4.3.2). Následně klient formátuje tyto požadavky do JSON struktur a přijímá odpovědi od .NET služby, čímž překlenuje mezeru mezi frontendem a backendem.

\noindent\begin{minipage}{\linewidth}
\lstinputlisting[language=C, stringstyle=\itshape, firstline=42, lastline=58]{../doc/code/unix_client.go}
\end{minipage}

\section{Pokročilé funkce a technické výzvy}
Během integrace stěžejních funkčních celků do architektury se objevilo několik netriviálních technických výzev, které vyžadovaly hlubší analýzu a návrh specializovaných řešení.

\subsection{Správa VPN spojení (OpenVPN)}
Samotné vytvoření šifrovaného tunelu skrze protokol OpenVPN těží z již existující architektury modulu \texttt{ProtoService}, který je nasazen na ostatních platformách. Pro potřeby CLI klienta tak stačilo mapovat požadavky z UDS rozhraní do podoby interních struktur sítě a volat sdílenou aplikační logiku. Modul automaticky vygeneruje odpovídající konfiguraci, sestaví spojení a na pozadí monitoruje stabilitu relace.

\subsection{Persistentní relace a vícero uživatelů}
Jednou z definovaných funkčností byla možnost navázání persistentního (trvalého) spojení, kdy VPN zůstává aktivní i po odhlášení aktuálního systémového uživatele nebo po restartu zařízení. Tento požadavek si vyžádal řešení problému týkajícího se izolace více Linuxových uživatelů. Jelikož je sestavené VPN spojení systémového charakteru (routování přes rozhraní tunelu ovlivňuje globální směrovací tabulky stroje), mohlo by dojít k situaci, kdy jeden uživatel sestaví persistentní spojení a jiný uživatel po přihlášení nemá oprávnění jej modifikovat či ukončit, přestože je jeho síťový provoz rovněž šifrován. Z toho důvodu bylo nutné obohatit komunikační protokol IPC o explicitní předávání systémového kontextu a identifikaci uživatele, který požadavek vznáší. Pokud je aktivní persistentní tunel spuštěný jiným uživatelem a nejedná se o autorizovanou operaci, je akce zamítnuta na úrovni služby. Administrátor pak musí relaci manuálně ukončit přes příkaz vyžadující superuživatelská práva, např. voláním \texttt{sudo ga-cli disconnect}.

\subsection{Implementace protokolu WireGuard}
Moderní protokol WireGuard představoval v kontextu architektury významnou výzvu, jelikož práce zahrnovala implementaci samotného agenta (sestavování sítě) cíleně pouze pro prostředí Linux s využitím statických konfigurací. Na rozdíl od uceleného řízení (životní cyklus přes operační systém, dynamické doporučování gateway, automatické znovupřipojení), které přesahuje rámec této bakalářské práce a je paralelně vyvíjeno ostatními členy týmu na různých platformách, se tato konkrétní implementace zaměřuje primárně na fundamentální logiku navázání komunikace přes nativní systémové nástroje (\texttt{ip link}, \texttt{wg}). Tyto operace jsou vloženy do kontextu služeb \texttt{ProtoService} a tvoří základ budoucí robustní síťové logiky.

\subsection{Vylepšení uživatelské přívětivosti (UX)}
S cílem maximalizovat komfort koncových uživatelů při interakci v terminálu byl klient postupně doplňován o podpůrné funkce a takzvané přepínače (flags). Příkladem je plná podpora pro strojově čitelné formátování výstupů prostřednictvím flagu \texttt{--json}, jenž zajišťuje, že aplikace nebude vracet interaktivní textové uživatelské rozhraní, ale naopak striktně strukturovaný JSON výstup. Tato funkcionalita je klíčová pro případnou integraci CLI nástroje do CI/CD řetězců (pipelines) a automatizačních skriptů, kde je vyžadováno bezobslužné řízení celého procesu připojení. Další UX optimalizace zahrnovaly vylepšení asynchronního logování stavu a přehlednější zpracování konfiguračních interakcí.
